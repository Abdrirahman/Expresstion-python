# Area of Circle is = 3.1415 * radius * radius
import math # math module

radius = float(input("Enter the radius"))

area = math.pi * radius * radius

print("Area of Circle si ", area)

r = float(input("Entr Radius"))
area = math.pi * r**2
print(area)